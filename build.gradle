apply plugin: 'groovy'

task wrapper(type: Wrapper) {
    gradleVersion = '2.2'
}

repositories { mavenCentral() }

dependencies {
    testCompile(
        'org.codehaus.groovy:groovy-all:2.+',
        'org.spockframework:spock-core:0.+',
    )
}

